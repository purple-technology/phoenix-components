import { Meta, Description, Canvas, ArgsTable, Story } from '@storybook/addon-docs'
import LinkTo from '@storybook/addon-links/react'

import { Grid } from '.'

<Meta
    title="components / Grid"
    parameters={{
        viewMode: 'docs'
    }}
/>

# Grid

Grid is a <LinkTo kind="components / Box">Box</LinkTo> component with `display` set to `grid` (can be overridden for example to `inline-grid`).

<Canvas withSource="open">
    <Story name="Grid">
        <Grid gridTemplateColumns="1fr 1fr" gridGap="20px">
            <Box>Item 1</Box>
            <Box>Item 2</Box>
        </Grid>
    </Story>
</Canvas>
