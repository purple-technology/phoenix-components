import { Meta, Description, Canvas, Story } from '@storybook/addon-docs'
import LinkTo from '@storybook/addon-links/react'

import Colors from './Colors'

<Meta
    title="Docs / Color Theme"
    parameters={{
        viewMode: 'docs'
    }}
/>

# Color Theme

Phoenix components use prop `colorTheme` across many components that have variable coloring. They are available in two basic variants - primary and secondary. Different options for this prop are:

<Story name="Color Theme">
	<Colors />
</Story>

**NOTE:** Some components (e.g. <LinkTo kind="components-button--default">Button</LinkTo>) allow only certain color themes: `brand`, `success`, `error` and `neutral`.
